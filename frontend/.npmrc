# react-scripts@5.0.1 declares a peer dependency on TypeScript ^4, but the project
# is on TypeScript 5.x (compiles and builds cleanly — CRA is deprecated and not
# updating its peer range). legacy-peer-deps lets npm install/ci resolve without
# failing on that stale peer constraint, in CI and locally alike.
legacy-peer-deps=true